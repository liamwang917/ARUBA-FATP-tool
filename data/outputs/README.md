# Outputs

Generated analysis outputs (for example, a `summary.xlsx` build worth
keeping for reference) go here when it's useful to check them into the
repository.

Not yet populated. By default, running the tool writes `summary.xlsx` next
to the raw-data folder you select (see `docs/usage.md`), and that output is
not checked into this repository.
