# Manifests

Dataset inventory / integrity manifests (per-dataset file lists, hashes,
counts) belong here.

Not yet populated. Until a per-dataset manifest format is defined here, see
`data/snapshots/README.md` for the current snapshot's SHA-256 hashes and
file-type counts.
